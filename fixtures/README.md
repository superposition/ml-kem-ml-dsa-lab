# Fixtures

Fixtures in this directory are repository-owned test inputs. They are not official NIST ACVP vectors.

`parameter-sets.json` mirrors the source-linked parameter derivation in `docs/parameter-tables.md` so future C++ and Rust tests can share the same names and byte-length expectations.


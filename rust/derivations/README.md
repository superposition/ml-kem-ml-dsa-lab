# Rust Derivations

This directory is for derivation notes that precede library code.

Each derivation note should answer:

- What object are we defining?
- Which FIPS section or table is the source?
- What invariant makes the object testable?
- What is the first red test?
- What implementation is the smallest honest green step?
- Which behavior must not be optimized before review?

